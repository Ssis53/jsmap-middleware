import { AbstractTools } from "../base/abstract-tools";
import { IToolOption } from "../protocol/tools/options";

export class GDTools extends AbstractTools {
    constructor(options: IToolOption) {
        super(options);
    }
}